
<div class="own-container">
  <%= render 'content' %>
</div>



<script>
  window.sr = ScrollReveal();
    // elements reveal
    sr.reveal('.scroll-reveal-box', { duration: 1000 ,reset: true });


    // knowledge-reveal
    var i = 300;
    $('.element-reveal-box').each(function(){
      sr.reveal($(this), {duration: i, reset: true});
        i += 300;
    });

  </script>
